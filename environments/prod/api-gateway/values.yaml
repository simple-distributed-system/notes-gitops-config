replicaCount: 3

image:
  repository: ghcr.io/sarka99/api-gateway
  tag: "ded297f-dirty"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-secret

podLabels:
  environment: prod

notesServiceUri: http://notes-service:80

resources:
  requests:
    memory: 256Mi
    cpu: 100m
  limits:
    memory: 512Mi
    cpu: 500m

autoscaling:
  enabled: false