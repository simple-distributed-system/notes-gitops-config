apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shared-secrets-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/simple-distributed-system/notes-gitops-config
    targetRevision: main
    path: environments/prod/shared/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: notes-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
