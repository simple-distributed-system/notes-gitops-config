apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notes-service-test
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/simple-distributed-system/helm-charts
      path: helm-notes-service
      targetRevision: main
      helm:
        valueFiles:
          - $values/environments/test/notes-service/values.yaml
    - repoURL: https://github.com/simple-distributed-system/notes-gitops-config
      targetRevision: main
      ref: values
    - repoURL: https://github.com/simple-distributed-system/notes-gitops-config
      targetRevision: main
      path: environments/test/notes-service/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: notes-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
